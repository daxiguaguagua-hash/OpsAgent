# 当前架构问题

- 基于json文件系统做的消息传递,比较笨重。
- 多个agent之间协调，靠的是30秒的定时器。
- 过于依赖 /goal + hooks ，这一套设计方案，适用性不够。
  - 如果以后，每个角色要更换为别的模型，那么有些模型的cli是没有 /goal + hooks的。

# 思考

- 尝试使用 OpenHands 来代替消息总线这个角色。
- 但是需要注意，OpenHands 是有企业版本的，这个需要付费。我们要注意！
