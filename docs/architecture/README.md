# Architecture（架构文档）

本目录收纳**已冻结**的架构说明文档，作为 GBrain RAG 的高权重知识来源。

## 当前文档

| 文件 | 说明 |
|---|---|
| `architecture-v3-candidacy.md` | v3 架构候选方案（评估中，尚未冻结） |

## 边界

- **仅收入**已通过评审、不再频繁变更的架构说明。
- **草案**与**评审中**文档请放入 `docs/decisions/` 或 `docs/devlog/`。
- **已废弃**文档移入 `docs/archive/`。

## 检索权重

高 — GBrain 优先检索本目录与 `docs/decisions/`。
